#include <stdio.h>
int main()
{
    int length ;
    int breadth ;
    printf("Enterteh value of length and breadth\n");
    scanf("%d %d",&length,&breadth);
    int area = length * breadth;

     printf("Area of rectagle is %d\n" , area);
 

    return 0;
}
